var s = jQuery;
s.noConflict();

s('.follow-unfollow').removeClass('flat-danger').addClass('flat-info').
attr('href', '<%= escape_javascript(user_follow_path(id: @user.id))%>').
text('FOLLOW');

s('#user-stats').html('<%= escape_javascript(render(:partial => "user_stats",  locals: {user: @user }))%>');