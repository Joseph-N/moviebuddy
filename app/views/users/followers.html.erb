<% title "#{@user.name} Followers" %>

 <section id="page-title-wrapper" class="section page-title-wrapper">
    <div class="container">
        <div class="row">
            <div class="col-xs-6 col-sm-6 col-md-6 page-title">
                <a href="javascript:history.go(-1)" id="ios-arrow-left">Back</a>                    
            </div>
            <!-- /.page-title -->
            <div class="col-xs-6 breadcrumb-wrapper">
                <% if @user == current_user %>
                    <h4 class="pull-right">Your Followers</h4>
                <% else %>
                    <h4 class="pull-right"><%= @user.name.split(" ").first %>'s Followers</h4>
                <% end %>
            </div>
            <!-- /.breadcrumb-wrapper -->
        </div>
        <!-- /.row -->
    </div>
    <!-- /.container -->
</section>
<section id="portfolio-2" class="section colored-wrapper">
    <div class="container">
        <div class="row">
            <div id="isotope-portfolio-container">
                <%= render :partial => 'user', :collection => @followers %>          	
            </div>
            <!-- /.isotope-portfolio-container -->
            <%= paginate @followers %>  
        </div>
        <!-- /.row -->
    </div>
    <!-- /.container -->
</section>