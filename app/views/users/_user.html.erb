<li>
	<%= link_to image_tag(user.avator.url(:thumb)), user_show_path(user) %>
    <h5>
    	<%= link_to user.name, user_show_path(user) %>
        <small><%= user.followers.count %> Followers | <%= user.movies.count %> Movies</small>
    </h5>
</li>